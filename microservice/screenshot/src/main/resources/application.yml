spring:
  application:
    name: web-generator-screenshot
  profiles:
    active: local
server:
  port: 8127
  servlet:
    context-path: /api
# Dubbo
dubbo:
  registry:
    register-mode: instance
  protocol:
    name: tri
    port: 50052
  consumer:
    timeout: 120000
  provider:
    timeout: 120000
  application:
    qos-enable: true
    qos-port: 0
