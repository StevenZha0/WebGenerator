spring:
  application:
    name: web-generator-screenshot
  profiles:
    active: local
server:
  port: 8127
  servlet:
    context-path: /api
# 添加 COS 对象存储配置（需要从腾讯云获取）
cos:
  client:
    host: https://codefree-ai-app-project-1455435167.cos.ap-guangzhou.myqcloud.com
    region: ap-guangzhou
    bucket: codefree-ai-app-project-1455435167
# Dubbo
dubbo:
  registry:
    register-mode: instance
  protocol:
    name: tri
    port: 50052
  consumer:
    timeout: 120000
  provider:
    timeout: 120000
  application:
    qos-enable: true
    qos-port: 0
